من الكتاب
# Chapter 2: Data Models and Query Languages

العنوان بالعربي: نماذج البيانات ولغات الاستعلام

الفكرة العامة للفصل:

الفصل ده بيشرح كيف تُنظّم البيانات وتخزنها، وإزاي نقدر نسترجعها بسرعة وكفاءة.

مش هدفه تعليم الكود أو SQL بالتفصيل، لكن الفكرة الكبرى لفهم تصميم الأنظمة.

ببساطة: كل تطبيق يعتمد على البيانات لازم يعرف شكل البيانات اللي هيخزنها وإزاي يقدر يبحث فيها أو يعدّلها.

## 1️⃣ Data Models – نماذج البيانات
أ) Relational (علائقية / SQL)

البيانات على شكل جداول مرتبطة ببعض (Rows & Columns).

مناسب للبيانات اللي عندها علاقات قوية.

مثال واقعي:

متجر إلكتروني: جدول Users، جدول Orders، جدول Products

ب) Document (وثائق / MongoDB)

البيانات على شكل مستندات JSON أو BSON.

مناسب للبيانات المرنة اللي شكلها مش ثابت.

مثال:

تطبيق دردشة، كل رسالة مستند منفصل يحتوي على النص، المستخدم، الوقت، إلخ.

ج) Key-Value (مفتاح-قيمة / Redis)

التخزين بسيط جدًا: مفتاح = قيمة.

أسرع للبحث المباشر عن مفتاح محدد.

مثال:

تخزين الجلسات (Session) في موقع ويب

د) Column-Family (مثل Cassandra)

مصمم للبيانات الضخمة الموزعة.

كل “عمود” ممكن يحتوي مجموعة كبيرة من البيانات.

مثال:

بيانات المستخدمين في شبكة اجتماعية ضخمة

💡 حاجة ممكن تبحث عنها: SQL vs NoSQL وDocument vs Key-Value

## 2️⃣ Query Languages – لغات الاستعلام

كل نموذج بيانات له لغة استعلام مناسبة:

SQL: البيانات العلائقية

MongoDB Query Language / XQuery: للوثائق أو XML

Key-Value APIs: لاسترجاع البيانات مباشرة عن طريق المفتاح

فكرة مهمة: اختيار لغة الاستعلام يعتمد على نوع البيانات والاستخدام المتوقع.

3️⃣ فهرسة البيانات (Indexes)

زي فهرس الكتاب، لتسريع البحث بدل ما تبحث في كل السطور.

مثال:

لو عندك 1 مليون مستخدم، Index على عمود Email يسرّع البحث بشكل كبير

💡 حاجة ممكن تبحث عنها: B-Tree vs LSM-Tree

## 4️⃣ Real-World Examples

Facebook / Twitter:

بيانات المستخدمين موزعة على وثائق (Document)

Redis for caching:

يستخدم Key-Value لتخزين الجلسات أو العدادات بسرعة

MySQL / Postgres:

استخدام البيانات العلائقية للحسابات والمعاملات

5️⃣##  الخلاصة

اختيار نموذج البيانات الصحيح يؤثر على:

الأداء (Performance)

سهولة التوسع (Scalability)

موثوقية النظام (Reliability)

أي نظام يعتمد على البيانات الكبيرة لازم يوازن بين:

نوع البيانات

لغة الاستعلام

طريقة التخزين والفهرسة
